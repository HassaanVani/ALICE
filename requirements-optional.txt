# Optional dependencies — ALICE degrades gracefully without these.
# All modules using these libraries wrap imports in try/except and fall
# back to simulation mode when unavailable.

mediapipe>=0.10.0    # vision/presence.py, vision/hand_detector.py — presence + gesture detection
open3d>=0.17.0       # vision/spatial_map.py — 3D TSDF reconstruction (no Python 3.13 wheel yet)
openai-whisper>=20230918  # voice_input.py — local speech-to-text
sounddevice>=0.4.6        # voice_input.py — microphone capture
